# Extensions

This directory is the source of truth for extensions published through NoX
Extension Manager.

Each extension must live in its own immediate subdirectory and include a
Manifest V3 `manifest.json` at that subdirectory's root.

